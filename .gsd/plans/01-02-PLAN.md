# Plan 01-02: Design Tokens & Theme Setup

## Tasks

<task type="auto">
  <name>Task 1: Setup Design Tokens & Tailwind Config</name>
  <files>web/tailwind.config.ts, web/src/app/globals.css</files>
  <action>
    Analyze `legacy/styles.css` and `legacy/css/design-system.css` to extract the "Cosmic/Elite" colors.
    - Deep Space Black: #050505
    - Nebula Purple: #6d28d9
    - Galaxy Silver/White: #f8fafc
    - Accent Gold/Cyan (from legacy).
    
    Update `tailwind.config.ts` with these as theme colors.
    Setup custom animations for "Glow" and "Pulse" using Tailwind's keyframes.
    Update `globals.css` with a smooth dark-to-black gradient background by default.
  </action>
  <verify>Check `tailwind.config.ts` for extracted colors.</verify>
  <done>Tailwind is configured with CM Tech Elite tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Elite Navbar & Footer (Base)</name>
  <files>web/src/components/layout/Navbar.tsx, web/src/components/layout/Footer.tsx, web/src/app/layout.tsx</files>
  <action>
    Build the premium navigation.
    - Navbar: Sticky, glassmorphism (backdrop-blur), logo on left, links in center, 'Fale Conosco' CTA on right.
    - Footer: Multi-column, clean, including international links.
    - Integrate them into the root `layout.tsx`.
    - Ensure all links point to future-proof paths.
  </action>
  <verify>Run dev server and see Navbar/Footer with glassmorphism.</verify>
  <done>Core layout structure is visible and responsive.</done>
</task>
