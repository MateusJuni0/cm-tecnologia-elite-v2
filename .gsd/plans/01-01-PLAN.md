# Plan 01-01: Project Foundation

## Tasks

<task type="auto">
  <name>Task 1: Initialize Next.js Project</name>
  <files>cm-tecnologia-elite-v2/</files>
  <action>
    Initialize a clean Next.js 15 project in the `cm-tecnologia-elite-v2/app-v2` (or root, cleaning up existing files) folder using:
    - App Router
    - TypeScript
    - Tailwind CSS
    - ESLint
    - src/ directory: No (use root app directory for simplicity in this project)
    - Import alias: @/*
    
    IMPORTANT: Since the repo already has files, we will create the Next.js app in a subdirectory `web` or move existing files to `legacy/` before initializing. I recommend moving all current files to a `legacy/` folder first.
  </action>
  <verify>Run `npm run dev` and check if the Next.js welcome page is accessible.</verify>
  <done>Next.js boilerplate is active and folder structure is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Setup Core Dependencies</name>
  <files>package.json</files>
  <action>
    Install essential libraries for the Elite stack:
    - framer-motion (animations)
    - lucide-react (icons)
    - next-intl (i18n)
    - clsx, tailwind-merge (styling utilities)
    - zod, react-hook-form (forms)
    - sonner (toasts)
  </action>
  <verify>Check package.json for the dependencies.</verify>
  <done>All core libraries are installed and ready to use.</done>
</task>
